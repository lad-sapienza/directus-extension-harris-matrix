var HmLog = require("../utils/hmlog.js");


const CarafaGraph = function(hdmj) {
    
    // Clustering O(NxE) - [usually less than O(NxN); O(NxN) if totally connected; more than O(NxE) if mothe than an edge between nodes]
    var graph = {}
    
    
    
    // Cycle check
    
    // TRED
    
    
    this.graph = graph;
    
    
    
    
}

module.exports = { CarafaGraph }
